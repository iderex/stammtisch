# internal

The server. Anything importable only by this module belongs here; anything a
third party is meant to write against does not, and goes in `botapi/`.

`docs/layout.md` is where the boundary between the packages under here is
argued, including the one the suite enforces.
